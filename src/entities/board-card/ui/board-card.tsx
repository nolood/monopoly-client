const BoardCard = ({ variant }: { variant: 'vertical' | 'horizontal' | '' }) => {
  return (
    <div className='bg-slate-700 text-white w-52 flex justify-center items-center mx-6 my-6'>
      dasda
    </div>
  );
};

export default BoardCard;
